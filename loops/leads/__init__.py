"""ICP lead qualification + enrichment loop.

See README.md for the verifier-first overview. Public entry points:
  - build(run_dir, budget, ...) -> wires generate + verify + state for run_loop
  - main()                      -> runs the loop against the bundled demo CSV
"""
